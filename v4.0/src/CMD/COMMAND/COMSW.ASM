;	SCCSID = @(#)comsw.asm	1.1 85/05/14
;	SCCSID = @(#)comsw.asm	1.1 85/05/14

include version.inc
include versiona.inc		;lpproj;

VER_RELAXED	EQU	TRUE	;lpproj;

$FOR_IBMPC = not 0		;lpproj; (not used for now)
$FOR_NEC98 = not 0		;lpproj; (not used for now)

