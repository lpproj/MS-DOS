	INCLUDE comsw.asm
	INCLUDE comseg.asm
	INCLUDE _tstamp.inc

ASSUME CS:TRANGROUP,DS:TRANGROUP

TRANSPACE	SEGMENT PUBLIC BYTE
IFDEF PATCH_REV_STR
		DB	13,10
		DB	'lpproj patch r', PATCH_REV_STR
	IFDEF BUILD_DATE_STR
		DB	' (built at ', BUILD_DATE_STR, ')'
	ENDIF
		DB	13,10,0
ENDIF

TRANSPACE	ENDS
	END
